import React, {Component} from 'react';
import {connect} from 'react-redux';
import SingleStation from '../components/SingleStation'

const mapPropsToState = (state) => ({});

export default connect(mapPropsToState)(SingleStation);
